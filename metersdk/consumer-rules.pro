# Keep MeterReadingSDK public API when the consuming app uses ProGuard/R8
-keep public class com.npcl.com.vcpopdl.MeterReadingSDK { public *; }
-keep public class com.npcl.com.vcpopdl.MeterReadingSDK$* { public *; }
